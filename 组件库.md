# 开发组件库时，如何保证 API 的向下兼容
1. 遵循语义化版本 (SemVer)
这是维护兼容性的基石。必须严格遵守 主版本号.次版本号.修订号 的规则：

修订号 (Patch): 内部修复，不涉及 API 变更。
次版本号 (Minor): 新增功能，但现有 API 保持不变（向下兼容）。
主版本号 (Major): 引入破坏性变更 (Breaking Changes)。

2. API 设计的“加法”原则
扩展而非修改： 尽量通过增加新的 Props 或参数来扩展功能，而不是修改现有 Props 的定义。
默认值保持一致： 如果新增了一个属性，确保它的默认值不会改变组件现有的行为逻辑。
接口收敛： 设计之初就应避免暴露过多的内部实现细节。暴露得越少，未来重构时的负担就越轻。

3. 优雅地废弃 (Deprecation Strategy)
标记废弃 (Warning)： 在次版本号更新中，使用 console.warn 提示用户该 API 已废弃，并告知替代方案。
  Tip: 可以使用 util.deprecate 或在文档中明确标注 [Deprecated]。
双重支持： 在一段时间内同时支持旧 API 和新 API，并在内部将旧 API 的调用映射到新逻辑上。
最终移除： 仅在下一个 Major 版本中彻底移除废弃的代码。

4. 技术层面的保障措施
静态类型检查 (TypeScript)
TypeScript 是防止非预期变更的神器。

  接口锁定： 使用 interface 定义组件的 Props，并通过自动生成的 d.ts 文件确保类型定义的连续性。

  严格模式： 开启严格的类型检查，防止开发者无意中修改了公共接口。

自动化回归测试
  快照测试 (Snapshot Testing)： 捕获组件渲染后的 DOM 结构。如果结构发生非预期变化，测试会报错。

  交互测试： 确保核心 API 的功能（如点击事件、数据流）在升级后依然符合预期。

  Visual Regression： 使用工具比对 UI 视觉差异，防止样式的非预期“断裂”。

Gemini 说
在组件库的生命周期中，向下兼容（Backward Compatibility） 是开发者与用户之间的一份隐形契约。一旦打破，用户升级的成本将变得巨大，甚至导致他们放弃使用你的库。

要保证 API 的向下兼容，可以从设计原则、版本管理、技术手段和流程规范四个维度入手。

1. 遵循语义化版本 (SemVer)
这是维护兼容性的基石。必须严格遵守 主版本号.次版本号.修订号 的规则：

修订号 (Patch): 内部修复，不涉及 API 变更。

次版本号 (Minor): 新增功能，但现有 API 保持不变（向下兼容）。

主版本号 (Major): 引入破坏性变更 (Breaking Changes)。

2. API 设计的“加法”原则
在开发新功能或优化旧功能时，优先考虑“只增不减”：

扩展而非修改： 尽量通过增加新的 Props 或参数来扩展功能，而不是修改现有 Props 的定义。

默认值保持一致： 如果新增了一个属性，确保它的默认值不会改变组件现有的行为逻辑。

接口收敛： 设计之初就应避免暴露过多的内部实现细节。暴露得越少，未来重构时的负担就越轻。

3. 优雅地废弃 (Deprecation Strategy)
当你确实需要重构某个 API 时，不要立即删除它，而是走“废弃路径”：

标记废弃 (Warning)： 在次版本号更新中，使用 console.warn 提示用户该 API 已废弃，并告知替代方案。

Tip: 可以使用 util.deprecate 或在文档中明确标注 [Deprecated]。

双重支持： 在一段时间内同时支持旧 API 和新 API，并在内部将旧 API 的调用映射到新逻辑上。

最终移除： 仅在下一个 Major 版本中彻底移除废弃的代码。

4. 技术层面的保障措施
静态类型检查 (TypeScript)
TypeScript 是防止非预期变更的神器。

接口锁定： 使用 interface 定义组件的 Props，并通过自动生成的 d.ts 文件确保类型定义的连续性。

严格模式： 开启严格的类型检查，防止开发者无意中修改了公共接口。

自动化回归测试
快照测试 (Snapshot Testing)： 捕获组件渲染后的 DOM 结构。如果结构发生非预期变化，测试会报错。

交互测试： 确保核心 API 的功能（如点击事件、数据流）在升级后依然符合预期。

Visual Regression： 使用工具比对 UI 视觉差异，防止样式的非预期“断裂”。

5. 流程与文档规范
编写变动日志 (Changelog)
清晰的 CHANGELOG.md 是必不可少的。它应该包含：

  New Features: 新增了什么。

  Bug Fixes: 修复了什么。

  Breaking Changes: 重点加粗，并提供迁移指南 (Migration Guide)。

持续集成 (CI)
在代码合并前，利用 CI 跑一遍所有的兼容性测试。

# 如何定义组件封装的边界？
1、区分基础组件和业务组件。基础组件（如 Button、Input）不带任何业务逻辑，只负责 UI 和交互；业务组件（如 UserSelect）则负责数据请求和特定业务流
2、如果把这个组件拿到另一个项目，还能直接用，那边界就是对的。