
# ES Module 和 CommonJS 什么区别
1、CommonJS 是运行时动态加载，ES Module 是编译时静态解析；
2、CommonJS 导出值拷贝，ES Module 导出引用绑定；
3、CommonJS 不支持 Tree Shaking，ES Module 支持。

# 闭包
闭包是内部函数引用外部函数变量并被保存执行形成的结构，能让外部函数作用域不被销毁。
主要用于：私有化变量、缓存状态、模块化、柯里化、循环保存变量、节流防抖。
优点是能持久化状态、实现封装；
缺点是使用不当可能造成内存泄漏。

